import 'package:flutter/material.dart';

class GynecologyPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Gynecology'),
        backgroundColor: Colors.teal,
      ),
      body: Center(
        child: Text('Gynecology Department'),
      ),
    );
  }
}