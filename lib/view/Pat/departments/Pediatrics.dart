import 'package:flutter/material.dart';

class PediatricsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Pediatrics'),
        backgroundColor: Colors.teal,
      ),
      body: Center(
        child: Text('Pediatrics Department'),
      ),
    );
  }
}