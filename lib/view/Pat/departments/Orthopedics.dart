import 'package:flutter/material.dart';

class OrthopedicsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Orthopedics'),
        backgroundColor: Colors.teal,
      ),
      body: Center(
        child: Text('Orthopedics Department'),
      ),
    );
  }
}