import 'package:flutter/material.dart';

class ENTPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('ENT'),
        backgroundColor: Colors.teal,
      ),
      body: Center(
        child: Text('ENT Department'),
      ),
    );
  }
}